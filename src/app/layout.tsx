import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Nidum AI - Next Generation AI Solutions",
  description: "Empowering the future with intelligent solutions. Experience the next generation of AI technology with Nidum AI.",
  keywords: "AI, artificial intelligence, machine learning, automation, analytics",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>{children}</body>
    </html>
  );
}
